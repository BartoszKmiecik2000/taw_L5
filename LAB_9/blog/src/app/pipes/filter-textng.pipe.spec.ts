import { FilterTextngPipe } from './filter-textng.pipe';

describe('FilterTextngPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTextngPipe();
    expect(pipe).toBeTruthy();
  });
});
